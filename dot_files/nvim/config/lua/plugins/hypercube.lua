-- Placeholder so `{ import = "plugins" }` always resolves, even when the host
-- override directory (~/.config/hypercube/nvim/lua/plugins/) is empty.
--
-- Do NOT add your customizations here -- this file is baked into the image and
-- replaced on upgrade. Put personal plugins in:
--   ~/.config/hypercube/nvim/lua/plugins/<anything>.lua
return {}
