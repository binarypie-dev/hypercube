# Hypercube Distrobox Configuration
# Pre-configured containers for development

[bluefin-cli]
image=ghcr.io/ublue-os/bluefin-cli:latest
pull=true
init=false
start_now=false
# Homebrew and common CLI tools pre-installed
# Access via: distrobox enter bluefin-cli
# Or use the 'brew' alias configured in fish
