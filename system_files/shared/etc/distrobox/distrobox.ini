# Hypercube Distrobox Configuration
#
# No pre-configured containers. The Neovim + AI development environment now
# ships as a standalone, portable podman image instead of a distrobox container
# (set up with 'ujust nvim-setup', launched via the 'nvim' wrapper).
#
# distrobox itself remains installed for ad-hoc toolboxes; add your own
# [container] sections here as needed.
