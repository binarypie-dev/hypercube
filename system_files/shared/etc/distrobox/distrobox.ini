# Hypercube Distrobox Configuration
# Pre-configured containers for development

[nvim-dev]
image=ghcr.io/binarypie-dev/nvim-dev:latest
pull=true
init=false
start_now=false
# Full neovim development environment with LSPs, formatters, linters pre-installed
# Enter: just nvim-dev
# Export to host: just nvim-export
