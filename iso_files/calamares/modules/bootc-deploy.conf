# Hypercube bootc Deploy Module Configuration
# Deploys the bootc image to the target system

# The container image to deploy
# This will be read from the running system
imageRef: ""

# Use the image from the currently running system
useCurrentImage: true

# Target root for installation
targetRoot: ""
